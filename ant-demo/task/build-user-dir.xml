<?xml version = "1.0"?>
<project basedir="${user.dir}" name="userdir" default="info">
   <target name="info">
      <echo message="basedir: ${basedir}"/>
      <echo message="user.dir: ${user.dir}"/>
   </target>
</project>
